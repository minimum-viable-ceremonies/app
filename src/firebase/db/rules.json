{
  "rules": {
    "rooms": {
      "$room_id": {
        ".read": "data.child('participants').hasChild(auth.uid)",
        ".write": "!data.exists()",
        "participants": {
          "$participant_id": {
            ".write": "$participant_id == auth.uid || !data.exists()"
          }
        }
      }
    }
  }
}
